server:
  port: 8004
spring:
  application:
    name:
      microservice-venta
  datasource:
    url:
      jdbc:postgresql://localhost:5432/microservice_venta_db
    username:
      postgres
    password:
      abcd1234
    driver-class-name:
      org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto:
        update
    show-sql:
      true
    properties:
      hibernate:
        format_sql:
          true
  cloud:
    config:
      enabled: false

springdoc:
  api-docs:
    enabled:
      true
    path:
      /v3/api-docs
  swagger-ui:
    enabled:
      true
    path:
      /swagger-ui.html